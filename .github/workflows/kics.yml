name: KICS IaC Scan

on:
  push:
    branches:
      - master
      - security-demo
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  kics:
    runs-on: ubuntu-latest

    steps:

      # 1. Checkout repository
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Create results directory
      - name: Create KICS results directory
        run: |
          mkdir -p kics-results

      # 3. Run KICS
      - name: Run KICS scan
        run: |
          docker run --rm \
            -v "${{ github.workspace }}:/path" \
            checkmarx/kics:latest \
            scan \
            -p /path \
            -o /path/kics-results \
            --report-formats "json,sarif" \
            --output-name kics-report

      # 4. Show generated files
      - name: Show KICS results
        if: always()
        run: |
          echo "======================================"
          echo "KICS RESULTS"
          echo "======================================"

          find kics-results -type f -print

          echo ""
          echo "======================================"
          echo "SARIF FILES"
          echo "======================================"

          find kics-results -type f -name "*.sarif" -exec ls -lh {} \;

      # 5. Verify SARIF
      - name: Verify KICS SARIF
        if: always()
        run: |
          SARIF_FILE=$(find kics-results -type f -name "*.sarif" | head -1)

          if [ -n "$SARIF_FILE" ]; then
            echo "SUCCESS: KICS SARIF generated:"
            echo "$SARIF_FILE"
            ls -lh "$SARIF_FILE"
          else
            echo "ERROR: KICS SARIF was not generated."
            echo "Files found:"
            find kics-results -type f
            exit 1
          fi

      # 6. Upload KICS results
      - name: Upload KICS results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kics-results
          path: kics-results/
          if-no-files-found: error
          retention-days: 30